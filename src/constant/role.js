/**
 * User Role Constants
 *
 * Defines the available user roles in the application.
 * Used for authorization (admin vs regular user).
 */

exports.ROLES = Object.freeze({
  USER: "user",
  ADMIN: "admin",
});
